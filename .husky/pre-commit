#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

cd backend

# Run type check
echo "Running type check..."
npm run type-check

# Run lint-staged (will lint and format staged files)
echo "Running lint-staged..."
npx lint-staged
